<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/recycler_image_view"
        android:layout_width="80dp"
        android:layout_height="80dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@drawable/shape_circle"/>

    <TextView
        android:id="@+id/recycler_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/recycler_image_view"
        app:layout_constraintBottom_toBottomOf="@+id/recycler_image_view"
        app:layout_constraintStart_toEndOf="@+id/recycler_image_view"
        android:layout_marginStart="10dp"
        android:text="이름입니다."/>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/bookmark_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        app:layout_constraintTop_toTopOf="@+id/recycler_image_view"
        app:layout_constraintBottom_toBottomOf="@+id/recycler_image_view"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/ic_bookmark_selector"/>

</androidx.constraintlayout.widget.ConstraintLayout>